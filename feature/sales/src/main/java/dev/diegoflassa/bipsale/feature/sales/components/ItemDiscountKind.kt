package dev.diegoflassa.bipsale.feature.sales.components

/** Which quantity a typed item-discount value means. Both cases are stateless, so this is an enum. */
enum class ItemDiscountKind { PERCENTAGE, AMOUNT }
